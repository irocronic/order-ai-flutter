name: makarna_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any

  cupertino_icons: ^1.0.2
  http: ^1.1.0
  shared_preferences: ^2.2.2
  jwt_decoder: ^2.0.1
  socket_io_client: ^2.0.3+1
  connectivity_plus: ^5.0.2
  provider: ^6.1.1
  flutter_dotenv: ^5.1.0
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.0
  firebase_storage: ^12.3.4
  google_sign_in: ^6.2.1
  flutter_native_splash: ^2.3.1
  flutter_ringtone_player: ^4.0.0+1
  animated_text_kit: ^4.2.2
  in_app_purchase: ^3.1.13
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.2
  uuid: ^4.3.3
  internet_connection_checker: ^1.0.0+1
  another_flushbar: ^1.12.30
  equatable: ^2.0.5
  qr_flutter: ^4.1.0
  flutter_staggered_grid_view: ^0.7.0
  fl_chart: ^0.66.2
  timezone: ^0.9.2
  flutter_timezone: ^1.0.8
  image_picker: ^1.0.7
  flutter_colorpicker: ^1.0.3
  table_calendar: ^3.0.9
  collection: ^1.18.0
  permission_handler: ^11.3.0
  esc_pos_printer_plus: ^0.1.1
  esc_pos_utils_plus: ^2.0.3
  network_info_plus: ^5.0.3
  network_discovery: ^1.0.0
  printing: ^5.11.1
  pdf: ^3.11.3
  google_fonts: ^6.2.1
  font_awesome_flutter: ^10.7.0
  excel: ^4.0.3
  open_file_plus: ^3.4.1
  flutter_blue_plus: ^1.31.20
  webview_flutter: ^4.7.0
  google_maps_flutter: ^2.12.0
  flutter_svg: ^2.0.7
  file_picker: ^6.1.1

  # YENİ EKLENDİ: Özel ses dosyalarını çalmak için.
  audioplayers: ^6.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
  generate: true
  assets:
    - .env
    # 🔊 BİLDİRİM SESLERİ (NOTIFICATION SOUNDS)
    - assets/sounds/notifications/tr/
    - assets/sounds/notifications/en/
    - assets/sounds/notifications/ar/
    - assets/sounds/notifications/de/
    - assets/sounds/notifications/es/
    - assets/sounds/notifications/fr/
    - assets/sounds/notifications/it/
    - assets/sounds/notifications/ru/
    - assets/sounds/notifications/zh/
    
    # 🎙️ KURULUM SESLERİ (SETUP WIZARD SOUNDS)
    - assets/sounds/setup/tr/
    - assets/sounds/setup/en/
    - assets/sounds/setup/ar/
    - assets/sounds/setup/de/
    - assets/sounds/setup/es/
    - assets/sounds/setup/fr/
    - assets/sounds/setup/it/
    - assets/sounds/setup/ru/
    - assets/sounds/setup/zh/
    - assets/templates/
    - assets/previews/
    - assets/fonts/

# --- flutter_native_splash İÇİN EKLENEN BÖLÜM ---
# Bu bölümü kendi projenize göre düzenlemeyi unutmayın.
flutter_native_splash:
  # Açılış ekranının arka plan rengi.
  color: "#000000" 

  # Açılış ekranında gösterilecek resmin yolu.
  # Projenizde "assets" klasörü içinde "splash.png" adında bir resim olmalı.
  # Kendi logonuzu kullanmak için bu yolu ve assets klasörünü düzenleyin.
  image: assets/splash.png

  # Android 12 ve sonrası için özel ayarlar.
  android_12:
    color: "#000000"
    image: assets/splash.png

  # Hangi platformlarda splash screen oluşturulacağını belirtir.
  android: true
  ios: true
  web: true